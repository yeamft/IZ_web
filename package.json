{
  "name": "iz-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npm start --prefix backend",
    "build": "cd frontend && npx cross-env CI=false npm run build"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}